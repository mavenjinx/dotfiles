return{
  "nvim-neorg/neorg",
  lazy = false, -- Disable lazy loading as some `lazy.nvim` distrobutions set `lazy = true` by default
  version = "*", -- Pin Neorg to the latest stable release
  config = true,
}
